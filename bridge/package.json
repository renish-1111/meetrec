{
  "name": "meetrec-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local bridge that receives Meet join/leave events from the MeetRec browser extension and drives OBS recording via obs-websocket.",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node ../test/bridge.test.mjs && node ../test/extension.test.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "obs-websocket-js": "^5.0.6"
  }
}
